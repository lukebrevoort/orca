{
  "name": "@orca/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "bun test",
    "lint": "tsc -p tsconfig.json",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun src/db/migrate.ts",
    "db:verify": "bun src/db/verify.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.15.0",
    "@orca/shared": "file:../../packages/shared",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.8.2"
  },
  "devDependencies": {
    "@types/bun": "^1.2.17",
    "drizzle-kit": "^0.31.10"
  }
}
